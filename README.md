# Python_basics_assignment
This submission has the assignment answers of the python


Explanation for Question 1:
Used a try-except block to handle invalid inputs, like strings or numbers out of range
Used a double if-else block to first validate the input is within the range 0-100 and then to determine the grade based on the score. 

Explanation for Question 2:
used a while loop to execute the infinite loop and can be exited by using the exit option, this can be achieved in saving the data as we are not saving the data persistetly

Explanation for Question 3:
used a file open to create and open a file, then used the file write command to enter data into the file
used file close to avoid data leaking and memory issues

used the CAT command to read the file content that was created in linux Kernel

Explanation for Question 4:
This code opens a file in read mode and prints its content to the console, then closes the file using the close() method
